'use strict';

module.exports.articleKeys = [`title`, `publicationDate`, `announce`, `fullText`, `category`];
module.exports.commentKeys = [`text`];
module.exports.categoryKeys = [`name`];

module.exports.entityNames = {
  ARTICLE: `article`,
  COMMENT: `comment`,
  CATEGORY: `category`
};
