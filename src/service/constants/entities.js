'use strict';

module.exports.articleKeys = [`title`, `createdDate`, `announce`, `fullText`, `category`];
module.exports.commentKeys = [`text`];

module.exports.entityNames = {
  ARTICLE: `article`,
  COMMENT: `comment`
};
