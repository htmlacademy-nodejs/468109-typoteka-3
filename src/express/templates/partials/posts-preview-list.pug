if (articles && articles.length > 0)
  section.main-page__list.preview
    h2.visually-hidden Список превью статей
    ul.preview__list
      each article in articles
        li(class=`preview__item ${!article.photo ? 'preview__item--noimage' : ''}`)
          ul.preview__breadcrumbs
            each category in article.categories
              li.preview__breadcrumbs-item
                a.preview__breadcrumbs-link(href=`/articles/category/${article.id}`) #{category.name}
          if (article.photo)
            .preview__background
              img.preview__background-image(src=`/img/${article.photo}` style="width: 460px; height: 240px" srcset=`/img/${article.photo} 1x` alt=(article.title))
          +formatDate('preview__time', article.publicationDate)
          h3.preview__name
            a.preview__name-link(href=`/articles/${article.id}`) #{article.title}
          p.preview__text #{article.announce}
          a.preview__comment(href=`/articles/${article.id}`)
            | Комментарии
            span.preview__cloud
            b.preview__comment-count #{article.comments.length}
else
  .main-page__empty-wrapper
    p.main-page__empty-text Здесь пока пусто!
